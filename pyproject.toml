[project]
name = "qa-lab"
version = "0.1.0"
description = "A dynamic test case manager."

readme = "README.md"
requires-python = ">=3.10"

[project.dependencies]
dependencies = "requirements.txt"

[project.optional-dependencies]
dev-requirements = {file = "dev-requirements.txt"}
